#![forbid(unsafe_code)]
//! The shared preview draw-list vocabulary (commands + visual identity): the
//! contract between the apps and the renderer. One source, renderers can't drift
//! (docs/decisions/D001.md).
