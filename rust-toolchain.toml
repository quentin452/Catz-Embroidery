# Pinned. Desktop suite — the Windows build is what ships, and reproducibility
# across machines is the point of the pin. See docs/CODE.md.
[toolchain]
channel = "1.92.0"
components = ["rustfmt", "clippy"]
targets = ["x86_64-pc-windows-msvc"]
profile = "minimal"
